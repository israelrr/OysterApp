// reducer for Cards

const dataCards = [];

const reducerDataCards = (state = dataCards, action)=>{
    switch (action.type) {
        case '':
            
            break;
    
        default:
            return 'bad request for data cards';
    }
};

export default reducerDataCards;